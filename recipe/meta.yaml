{% set name = "ophyd" %}
{% set version = "1.3.1" %}
{% set sha256 = "30e8b47aacc4410ae14bdc3de8fd9d6f795b0d2b55dc276d3eac9f0ed0708649" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: ophyd-v{{ version }}.tar.gz
  url: https://github.com/NSLS-II/ophyd/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 1000
  script: python setup.py install --single-version-externally-managed --record record.txt
  skip: True  # [py2k or win]

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - networkx
    - numpy

test:
  imports:
    - ophyd
    - ophyd.utils
    - ophyd.areadetector
    - ophyd.sim

about:
  home: https://github.com/NSLS-II/ophyd
  license: BSD 3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: 'Ophyd - EPICS Client Abstractions'

extra:
  recipe-maintainers:
    - CJ-Wright
